<%- include('partials/header') %>

<div>
    <h2> Votre score est de : <%= score %> </h2>

        <h3> Les reponses suivantes etaient correctes: </h3>
            <ul>
                <%for (let answer of goodAnswers) { %>
                <li> <%= answer %> </li>
                <%} %>
            </ul>

        <h3> Les reponses suivantes etaient incorrectes: </h3>
            <ul>
                <%for (let answer of badAnswers) { %>
                <li> <%= answer %> </li>
                <%} %>
            </ul>

</div>


<%- include('partials/footer') %>